# Conversation Log — 2026-05-06 10:28

# 참여자
- Toby (사용자)
- Claude (Opus 4.7 — 1M context)

## 브랜치
- `main`

---

## 1. 기존 skill/hook 전체 리뷰 및 개선
사용: Explore agent, Read, Bash, Edit, Write, TaskCreate/TaskUpdate

Toby: `toby-essentials` 플러그인의 skill 18개와 hook 2개를 모두 리뷰하고 개선점을 모두 수정해 달라.

Claude:
- Explore agent로 전체 audit 수행 — Critical/Important/Polish로 분류해 보고.
- Toby가 범위 B (Critical+Important+Polish, 외부 사실 검증 제외) 선택.
- 9개 fix 작업으로 쪼개 TaskCreate로 추적, 순차 진행.
- **Hook (`block-dangerous.sh`)** 재작성:
  - regex 강화 — `-R`, `--recursive`, `--no-preserve-root` 모두 차단, `-f` 단독은 허용.
  - `python3` 부재 시 `python` → `realpath` → 셸 폴백 chain 추가.
  - `set -uo pipefail`, `resolve_path` 헬퍼 함수로 정리.
  - 5개 케이스 smoke test 통과 확인 (BLOCKED 3개, ALLOWED 2개).
- **Hook (`save-conv-before-commit.sh`)** 재작성:
  - 출력 프로토콜 stdout-JSON + exit 0으로 전환 (현 Claude Code spec).
  - `git commit --amend`는 통과, staleness 3분→5분 완화.
  - `xargs -0 ls -t` 패턴 → `find -print0` + `stat` loop으로 안전화.
- **Skill 7개 수정**:
  - `toby-codex`/`toby-gemini`: `$(pwd)` → `git rev-parse` 폴백, prompt heredoc 제거, multiline `cmux send` 안전 패턴 명시, timeout `TOBY_*_TIMEOUT_SEC` 환경변수화.
  - `toby-team-starter`: `cmux send "...\n"` → `send` + `send-key enter` 분리.
  - `save-conversation`: git repo 아닐 때 `pwd` 폴백, `xargs ls -t` 제거.
  - `tdd-team`: "Test already passes → skip GREEN" 안티패턴 → STOP + 사용자 확인.
  - `prd`: YAML `>` 폴드, 한국어 trigger 9개 추가, 버전 1.0.0→0.2.0.
  - `claude-backup`: description 단일라인 → 폴드 블록 (스타일 일관성).
  - `_template`: 한국어 trigger 예시 + 플러그인 컨벤션 섹션 추가.
  - `omc-tips`: 하드코딩된 카운트 ("19 agents, 37 skills") → live `omc list-*` 안내.
- 11개 파일 / +246 / -155 변경. 범위 밖으로 미뤄둔 항목 (Codex/Gemini CLI 실제 플래그, Spring Boot 4 사실 검증, `cmux send` `\n` 동작, toby-codex↔toby-gemini 공통 reference 추출) 명시.

---

## 2. 버전 bump (`upgrade`)
사용: Read, Edit, Bash, Skill (save-conversation)

Toby: `upgrade`

Claude:
- CLAUDE.md의 `upgrade` 절차 따라 1.30.0 → 1.31.0 minor bump.
- `plugins/toby-essentials/.claude-plugin/plugin.json` version 업데이트.
- `README.md`의 `### toby-essentials` 헤더 버전 태그 업데이트.
- `CHANGELOG.md`에 `[1.31.0] - 2026-05-06` Fixed/Changed 섹션 추가.
- 14개 파일 staging 후 commit 시도 — `save-conv-before-commit.sh` hook이 stale (3분 초과)으로 차단.
- save-conversation skill 실행 (이 파일).

---

## 변경된 파일
- `CHANGELOG.md` (수정)
- `README.md` (수정)
- `plugins/toby-essentials/.claude-plugin/plugin.json` (수정)
- `plugins/toby-essentials/hooks/scripts/block-dangerous.sh` (수정)
- `plugins/toby-essentials/hooks/scripts/save-conv-before-commit.sh` (수정)
- `plugins/toby-essentials/skills/_template/SKILL.md.template` (수정)
- `plugins/toby-essentials/skills/claude-backup/SKILL.md` (수정)
- `plugins/toby-essentials/skills/omc-tips/SKILL.md` (수정)
- `plugins/toby-essentials/skills/prd/SKILL.md` (수정)
- `plugins/toby-essentials/skills/save-conversation/SKILL.md` (수정)
- `plugins/toby-essentials/skills/tdd-team/SKILL.md` (수정)
- `plugins/toby-essentials/skills/toby-codex/SKILL.md` (수정)
- `plugins/toby-essentials/skills/toby-gemini/SKILL.md` (수정)
- `plugins/toby-essentials/skills/toby-team-starter/SKILL.md` (수정)
- `conv-logs/202605/06/conv-20260506-102811.md` (추가)
